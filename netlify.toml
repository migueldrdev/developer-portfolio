[build]
  # Comando estándar de Astro
  command = "npm run build"
  # Carpeta donde Astro tira el HTML
  publish = "dist"

[functions]
  # 👈 ESTO ES LO IMPORTANTE
  # Le decimos explícitamente dónde buscar las funciones
  directory = "netlify/functions"